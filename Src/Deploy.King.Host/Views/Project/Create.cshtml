@model Deploy.King.Host.Models.Project.SelectProcedureType
@{
    ViewBag.Title = "Select procedure";
}

<p>
    @using (Html.BeginForm("CreateArguments", "Project"))
    {
        @Html.DropDownListFor(m => m.ProcedureType, Model.ProcedureTypes)

        <p>
            <input type="submit" value="Create" />
        </p>
    }
</p>
